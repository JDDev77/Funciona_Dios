<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detalles del Alquiler</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
          background: linear-gradient(to right, #FFD700, #8B4513);
          color: #000000
      }
      img{
          width: 400px
      }
    </style>
  </head>
  <body class="container mt-5">
    <div class="card p-4 shadow">
      <h1 class="mb-4">Detalles del Alquiler</h1>

      <ul class="list-group mb-3">
        <li class="list-group-item">
          <strong>NIF:</strong> <%= alquiler.NIF %>
        </li>
        <li class="list-group-item">
          <strong>Precio:</strong> <%= alquiler.precio %>
        </li>
        <li class="list-group-item">
          <strong>Marca:</strong> <%= alquiler.marca %>
        </li>
        <li class="list-group-item">
          <strong>Modelo:</strong> <%= alquiler.modelo %>
        </li>
        <li class="list-group-item">
          <strong>Fecha de Recogida:</strong> <%= alquiler.fecha_recogida %>
        </li>
        <li class="list-group-item">
          <strong>Fecha de Devolución:</strong> <%= alquiler.fecha_devolucion %>
        </li>
        <li class="list-group-item">
          <strong>Imagen:</strong><br /><img src="<%= alquiler.URL %>" />
        </li>
      </ul>
      <% if(usLogin != null){ %> <%if(usLogin.profile == "ADMIN"){%>
      <form
        action="/api/v1/alquileres/<%= alquiler.NIF %>?_method=DELETE"
        method="POST"
      >
        <button type="submit" class="btn btn-danger">Eliminar</button>
      </form>
      <% } %> <% } %>
      <a href="/index" class="btn btn-secondary mt-3"
        >Volver al listado de Alquileres</a
      >
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
