<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Página de Inicio</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      @keyframes fondoMovimiento {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }
      html,
      body {
        margin: 0;
        padding: 0;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        background: linear-gradient(-45deg, #000, #900, #000);
        background-size: 400% 400%;
        animation: fondoMovimiento 15s ease infinite; /* Asegura que el html y body ocupen todo el ancho de la pantalla */
      }
      body {
        background: linear-gradient(-45deg, #000, #900, #000);
        background-size: 400% 400%;
        animation: fondoMovimiento 15s ease infinite;

        margin: 0;
        height: 100vh;
        overflow: hidden;
      }

      .col,
      h1 {
        font-family: "Arial", sans-serif; /* Ejemplo de tipo de fuente */
        color: #900; /* Color rojo oscuro para el texto */
        text-transform: uppercase; /* Transforma el texto a mayúsculas */
        letter-spacing: 2px; /* Aumenta el espaciado entre letras */ /* Borde blanco para la caja */
        margin: 20px; /* Espacio alrededor de cada caja */
        padding: 20px; /* Espacio interno para que el contenido no toque los bordes */
        border-radius: 10px; /* Bordes redondeados */
        width: auto; /* O especifica un ancho */
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.5); /* Sombra suave alrededor de la caja */
        background-color: rgba(0, 0, 0, 0.6);
        text-shadow: 2px 2px #000; /* Sombra negra para darle profundidad al texto */
      }

      #imagenCoche,
      #imagenMoto {
        /* Fondo blanco para las imágenes */
        padding: 10px; /* Espacio interno para hacer visible el fondo blanco */
        margin: 10px auto; /* Margen arriba y abajo, y centra horizontalmente */
        display: block; /* Asegura que la imagen se trate como un bloque para centrar */
        border-radius: 20px;
      }
      .fotos {
        display: flex;
        justify-content: space-between;
        gap: 100px;
        max-width: 100%;
        height: auto;
        object-fit: cover; /* Espacio interno para hacer visible el fondo blanco */ /* Margen arriba y abajo, y centra horizontalmente */
      }
      .log {
        max-width: 100%; /* Asegura que la imagen no sobrepase el ancho del contenedor */
        height: auto;
        max-width: 100%;
        height: auto;
        object-fit: cover;
      }
      @media (max-width: 768px) {
        .log,
        .fotos,
        .col,
        .h1 {
          flex-direction: column; /* Cambia a disposición vertical en pantallas pequeñas */
        }
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h1>BIENVENIDOS A ROADRUNNERS RENTAL COMPANY</h1>
      <div class="logo">
        <img
          src="./img/Log.png"
          class="img-fluid imagenCoche"
          alt="Coche"
          style="max-width: 400px"
        />
      </div>
      <h1>¿Que deseas alquilar?</h1>
      <!-- Contenedor del coche -->
      <div class="fotos">
        <div class="col-md-6 text-center mb-4" id="imagenCoche">
          <a href="/index"
            ><img
              src="./img/Coche-removebg-preview.png"
              class="img-fluid imagenCoche"
              alt="Coche"
              style="max-width: 300px"
          /></a>
        </div>

        <!-- Contenedor de la moto -->
        <div class="col-md-6 text-center mb-4" id="imagenMoto">
          <a href="http://localhost:3000"
            ><img
              src="./img/Moto-removebg-preview.png"
              class="img-fluid"
              alt="Moto"
              style="max-height: 300px"
          /></a>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
