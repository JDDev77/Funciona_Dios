<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mostrar Usuario</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
          background: linear-gradient(to right, #36D1DC, #5B86E5);
          color: #000000
      }
    </style>
  </head>
  <body class="container mt-5">
    <div class="card p-4 shadow">
      <h1><%= usuario.nombre %> <%= usuario.apellidos %></h1>

      <ul class="list-group mb-3">
        <li class="list-group-item">
          <strong>NIF:</strong> <%= usuario.nif %>
        </li>
        <li class="list-group-item">
          <strong>Nombre:</strong> <%= usuario.nombre %>
        </li>
        <li class="list-group-item">
          <strong>Apellidos:</strong> <%= usuario.apellidos %>
        </li>
        <li class="list-group-item">
          <strong>Fecha de Nacimiento:</strong> <%= usuario.fechaNac %>
        </li>
      </ul>

      <a href="/area-personal" class="btn btn-secondary"
        >Volver al listado de usuarios</a
      >
      <% if(usLogin.profile == "ADMIN"){%>
      <form
        action="/api/v1/usuarios/<%= usuario.nif %>?_method=DELETE"
        method="POST"
        class="d-inline"
      >
        <button type="submit" class="btn btn-danger mt-3">Eliminar</button>
      </form>
      <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
